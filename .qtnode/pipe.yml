init:
  - pkg: console

dev:
  - middleware: devserver

build:
  - middleware: eslint
  - middleware: mocha
  - middleware: webpack

test:
  - middleware: eslint
  - middleware: mocha

publish:

config:
  lanague: ts